# Cross-validation audit findings register (v1)

**Audit date:** 2026-07-14
**Issue:** #139
**Reviewed change:** `a8ead627` against `15ff5eb4`

The register records confirmed findings from the complete PR #137 review.
Line evidence refers to commit `a8ead627` unless a later correction updates the entry.

| ID | Severity | Category | Evidence | Impact | Correction | Owner | Disposition |
|---|---|---|---|---|---|---|---|
| CV-001 | High | External-store invalidation | `R/Pipelines/pipeline_sjsdm_tier_tuning.R:134-172` | The always-cued discovery target returns stable store path strings, so the downstream summary target can remain cached after tuning summaries inside those stores change. A new creation timestamp can therefore republish stale tier losses and selections. | Make summary collection respond to external-store content, either by always cueing the collection target or by introducing explicit metadata/hash dependencies for each source summary. Add a regression test for changed source content at a stable path. | Pipeline/orchestration | Resolved in initial Issue #139 slice: summary collection is always-cued and guarded by `test-tier_tuning_pipeline_contract.R`. |
| CV-002 | Medium | Repeated spatial assignment | `R/Functions/Modelling/Cross_validation/make_spatial_cross_validation_assignments.R:233-255` | Multiplication by two is not coprime with even repeat counts. For two repeats the y-origin never changes, and for other even counts only half of the requested y offsets are used, weakening boundary-sensitivity coverage. | Use a deterministic coprime permutation or another explicit two-dimensional origin design, and test shift uniqueness and coverage for even repeat counts. | Cross-validation functions | Resolved: the reverse-order multiplier is coprime with every repeat count and covers every y-origin once; the four-repeat regression test asserts the exact expected signature sequence. |
| CV-003 | Medium | Deterministic tuning score/provenance | `R/Functions/Modelling/Cross_validation/score_sjsdm_joint_tuning_predictions.R:150-198`; `R/Functions/Modelling/Cross_validation/run_sjsdm_tuning_fold_candidates.R:255-269` | Stochastic likelihood draws have no explicit scoring seed or RNG restoration. Candidate ranking can depend on ambient backend state or execution order, while provenance records only the fit seed. | Derive and apply a deterministic score seed for each repeat/fold/candidate, restore caller RNG where applicable, and record the score seed in tuning output and provenance. | Cross-validation functions | Resolved: fit and score seeds are separately hashed from the base seed plus repeat/fold/candidate ID; joint scoring seeds and restores R plus available PyTorch CPU/CUDA RNG states. Regression tests cover candidate-order independence, deterministic R/PyTorch draws, normal/error caller-state restoration, seed bounds/propagation, and schema. |
| CV-004 | Medium | Regularization input contract | `R/Functions/Modelling/Fitting/fit_jsdm_model.R:324-373` | Direct final-fit/API calls can pass alpha mixing values greater than one to sjSDM because only a lower bound is enforced. | Validate each `alpha_*` in the closed interval `[0, 1]`; continue validating each `lambda_*` as finite and non-negative. Add direct-call boundary tests. | Model fitting | Resolved: direct validation now enforces all three alpha parameters in `[0, 1]`, preserves non-negative lambda contracts, and has parameter-specific lower-, upper-, and inclusive-boundary tests. |
| CV-005 | Medium | Generated documentation contract | Deleted `R/Functions/Modelling/Decomposition_diagnostics/run_predictive_ablation_cv.R` and its test remain represented by `Documentation/Functions/run_predictive_ablation_cv.{html,pdf,txt}`, the function website QMD, and the published site page. | Public documentation advertises a deleted API. | Regenerate function documentation and the website, then verify the deleted function is absent from source and published inventories. | Documentation | Open; correct during stabilization. |
| CV-006 | Medium | Generated documentation contract | Deleted `apply_decomposition_scale_attributes()` remains published under `docs/`, while the replacement `apply_scale_attributes()` is not published there. | The published API inventory disagrees with source and generated function documentation. | Regenerate and verify the website after source documentation is stabilized. | Documentation | Open; correct during stabilization. |
| CV-007 | Medium | Generated PDF contract | The HTML/TXT/QMD documentation for `interpolate_mev_to_grid()` and `interpolate_st_mev_to_grid()` describes optional unscaled output, but their PDFs are unchanged from the former always-scaled contract. | Different generated formats describe incompatible public behavior. | Regenerate both PDFs from the current roxygen2 sources and compare all generated formats. | Documentation | Open; correct during stabilization. |
| CV-008 | High | Tier evidence completeness and provenance | `R/Functions/Modelling/Cross_validation/collect_sjsdm_tuning_summaries.R:62-101` | Individual target-read failures are converted to `NULL` and silently removed. Tier completeness is then evaluated only over surviving sources, so an apparently complete artifact can omit requested stores/resolutions and select a different candidate without recording the missing evidence. | Materialize a status for every requested store-resolution pair; distinguish missing, read error, invalid, explicit exclusion, and available states; require the approved evidence policy before selection; carry requested/available/excluded IDs and counts into provenance. | Tier tuning/store orchestration | Resolved with a fail-closed policy in the initial slice: any unreadable requested pair aborts with target/store evidence; successful output schema is unchanged. Covered by `test-collect_sjsdm_tuning_summaries.R`. |
| CV-009 | Medium | Spatial runner failure policy | Stage-A tuning loops in the six spatial runners, including `R/02_Main_analyses/01_Spatial/01_Paleo/02_Run_spatial_regional.R:60` and its modern/regional/local counterparts. | One failed unit aborts the entire tuning stage and prevents other units from reaching tier aggregation/final fitting, while later regional/local loops intentionally continue per-unit and the collector currently skips unavailable summaries. | Define one explicit failure policy. If continuing, collect per-unit errors and enforce an explicit evidence-completeness policy before aggregation; otherwise make fail-fast consistent and documented across both stages. | Runner/orchestration | Open; resolve together with CV-008. |
| CV-010 | Medium | Cross-tier sensitivity lifecycle | `R/02_Main_analyses/01_Spatial/01_Paleo/03_Run_spatial_local.R:117`, modern counterpart, and `R/Pipelines/pipeline_sjsdm_common_regularization_sensitivity.R:162-168` | A local runner invoked independently errors after completing local work when continental/regional roots or representative stores do not yet exist. | Use a dedicated all-tier orchestrator or preflight every required representative store and publish an actionable skipped/incomplete status. | Runner/orchestration | Open; correctness/operational stabilization required. |
| CV-011 | Medium | MEM feasibility and provenance | `R/Functions/Modelling/Cross_validation/make_cross_validation_partition_diagnostics.R:321`; `R/Functions/Modelling/Cross_validation/summarise_sjsdm_model_provenance.R:112-114` | Feasibility does not account for requested or constructible positive MEM rank, while MEM construction can clamp rank per fold and provenance aborts if effective ranks vary. A feasible run can therefore fit successfully and fail only when provenance is assembled. | Make feasibility rank-aware or explicitly accept fold-varying rank; record per-fold/min/max effective MEM counts and a clear status instead of forcing one count. | Fold preparation/provenance | Open; correctness stabilization required. |
| CV-012 | Medium | Tuning metric estimand | `R/Functions/Modelling/Cross_validation/summarise_sjsdm_tuning_candidates.R:178-185` | The field named `negative_log_likelihood_per_response` is an unweighted mean of fold-normalized losses, not total NLL divided by total response values. Unequal fold sizes or retained-taxon counts therefore overweight smaller folds while downstream artifacts describe a pooled per-response estimand. | Compute the pooled ratio, or explicitly approve and rename the macro-fold estimand; add an unequal-fold regression test before changing scientific output. | Tuning metrics/scientific owner | Open; explicit scientific disposition required before optimization. |
| CV-013 | Medium | Structured error-status coverage | `R/Functions/Modelling/Cross_validation/run_sjsdm_tuning_fold_candidates.R` and `test-run_sjsdm_tuning_fold_candidates.R` | The implementation returns `prediction_error` and `scoring_error`, but the PR test suite contains no assertions for either branch. Contract regressions could silently change status or error-message propagation. | Inject prediction and scoring failures and assert the exact structured status/message/schema. | Cross-validation tests | Resolved: injected failures now assert exact schema, status, message, missing metrics, seed provenance, and prediction short-circuiting; the fold helper was normalized to the public tuning column order. |
| CV-014 | Medium | Test-evidence freshness | `Documentation/Functions_test_coverage/covr_report.html` | The report still lists deleted functions and omits new/replacement CV functions, so it cannot serve as current test evidence. | Regenerate the coverage report after source and test stabilization and verify deleted/new API membership. | Documentation/testing | Open; regenerate near closure. |
| CV-015 | Low | Target declaration maintainability | `R/Pipelines/pipeline_sjsdm_tier_tuning.R:137-157` | A multi-statement `tar_target()` command conflicts with the repository target-declaration convention and is difficult to test directly. | Defer helper extraction to Issue #141 unless a correctness fix requires the same boundary. | Pipeline cleanup | Deferred candidate for #141; requires explicit acceptance/link. |
| CV-016 | Low | Function helper structure | Nested named helpers in `run_sjsdm_selected_candidate_folds.R`, `prepare_fold_spatial_predictors.R`, and `prepare_model_fold_input.R`. | Helpers are not independently documented/testable and conflict with one-function-per-file guidance. | Extract only during the maintainability phase unless touched by a correctness fix. | Function cleanup | Deferred candidate for #141; requires explicit acceptance/link. The nested validation helper in `fit_jsdm_model.R` was removed while resolving CV-004. |
| CV-017 | Medium | Stale package-native documentation | `R/Functions/Modelling/Decomposition_diagnostics/make_repeated_cv_indices.R:3-4` and generated documentation | The helper still claims its output is for `sjSDM::sjSDM_cv()`, while current consumers use project-owned decomposition CV and package-native orchestration was removed. | Update the source contract to name the project-owned decomposition runner and regenerate all formats. | Function documentation | Open; correct during stabilization. |
| CV-018 | Low | Touched test style | New/touched lines in `test-fit_jsdm_model.R`, `test-run_sjsdm_common_regularization_sensitivity.R`, and `test-read_spatial_model_results.R` use bare base calls or `$` access. | The tests conflict with namespace and data-frame access conventions. | Qualify base calls and use `[[...]]` or `dplyr::pull()`. | Tests | Open; non-blocking style correction. |
| CV-019 | Low | Roxygen example coverage | Fourteen reviewed function files lack `@examples`, including several new CV runner/scoring functions. | Public/internal API documentation is incomplete relative to repository conventions. | Add focused examples when regenerating function documentation. | Function documentation | Deferred candidate for documentation cleanup; correctness contracts take priority. |
| CV-020 | Low | Test fixture organization | Nine new test files begin with top-level fixture helpers. | Fixtures persist in the shared test environment and conflict with the preferred test-file structure. | Localize fixtures or add a clear fixture section during test cleanup. | Tests | Deferred candidate for #141 unless a touched test makes cleanup trivial. |
| CV-021 | Low | Diagnostic namespace style | `R/03_Supplementary_analyses/_Diagnostics/Diagnose_spatial_pipeline_unit.R:155-157` retains bare base calls in touched code. | The diagnostic conflicts with explicit namespace conventions. | Qualify touched base calls. | Diagnostics | Open; non-blocking style correction. |

## Disposition rules

- High and medium correctness or contract findings remain blocking until corrected and validated.
- Maintainability-only findings may be deferred only with an explicit link and accepted disposition to Issue #141.
- New evidence is appended under a stable finding ID; resolved entries are not deleted.
