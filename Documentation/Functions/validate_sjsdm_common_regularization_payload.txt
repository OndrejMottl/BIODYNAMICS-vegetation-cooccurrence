Validate the sjsdm common regularization Payload

Description:

     Applies the artifact-specific registered payload contract.

Usage:

     validate_sjsdm_common_regularization_payload(payload = NULL)

Arguments:

 payload: Named artifact payload.

Value:

     Invisible `TRUE`; invalid payloads abort.
