Load Functional-Type Classification

Description:

     Reads an explicit functional-type classification `.qs` file.

Usage:

     load_functional_type_classification(path_classification_file)

Arguments:

path_classification_file: A single readable `.qs` file path.

Value:

     A tibble with columns `taxon_name` and `functional_type`.
